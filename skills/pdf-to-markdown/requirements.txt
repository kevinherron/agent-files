# pdf-to-markdown — Python dependencies
#
# Install:   python3 -m pip install -r requirements.txt
#   (on an externally-managed Python — Debian/Ubuntu, Homebrew — either use a
#    virtualenv, or add --break-system-packages.)
#
# These cover only the helper SCRIPTS. The conversion itself is driven by the
# Workflow tool + agents and needs no Python packages.

# --- Core (triage / planning) -------------------------------------------------
# pdf_probe.py, pdf_pagemap.py: page count, text-layer coverage, embedded
# outline, born-digital/scanned verdict, redline/multilingual page classification.
pypdf>=4.0

# --- Optional: figure-extraction phase only -----------------------------------
# extract_figures.py: render+crop vector figures, pixel whitespace segmentation.
# (inject_figures.py is stdlib-only.) Skip these if you don't run the figure phase.
pymupdf>=1.24      # imported as `fitz`
pillow>=10.0       # imported as `PIL`
numpy>=1.26

# --- System (NOT pip-installable) ---------------------------------------------
# The Poppler command-line tools are required by pdf_probe.py and used by convert
# agents to cross-check text (pdftotext, pdfinfo, pdftoppm, pdfimages):
#   macOS:          brew install poppler
#   Debian/Ubuntu:  apt-get install poppler-utils
